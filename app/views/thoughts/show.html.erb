<!DOCTYPE html>
<html lang="ru">
  <head>
    <style>
      body{
        background-color: #9370DB;
      }
      header {
	background-color: #E6E6FA;
	text-align: center;
	width: 100%;
	height: auto;
	background-size: cover;
	background-attachment: fixed;
	position: relative;
	overflow: hidden;
	border-radius: 0 0 85% 85% / 30%;
}
 
      h2{
        color: black;
        font-family: 'Dancing Script', cursive;
	      font-size: 20px;
	      margin-bottom: 30px;
        text-align: center;
      }
    </style>
  </head>
    </body>
<h1><%= @thought.title %></h1>
  <p><%= @thought.body %></p>
  <%= link_to "Редактировать", edit_thought_path(@thought), :class => 'btn btn-warning' %>
    <%= link_to "Удалить", thought_path(@thought), method: :delete, data: {confirm: "Эти мысли больше не нужны?"}, :class => 'btn btn-danger' %>
    </body>